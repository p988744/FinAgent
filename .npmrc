# NPM Configuration for FinAgent Electron
# Helps with cross-platform builds

# Use the public npm registry
registry=https://registry.npmjs.org/

# Save exact versions in package.json
save-exact=true

# Don't save optional dependencies
save-optional=false

# Prefer offline packages if available
prefer-offline=true
