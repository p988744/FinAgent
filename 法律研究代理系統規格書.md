# 法律研究代理系統規格書
## 銀行罰款與監管執法自主研究代理系統

**版本：** 1.0
**日期：** 2025年11月11日
**基於：** Dexter 金融研究代理架構
**適用地區：** 台灣

---

## 1. 執行摘要

### 1.1 系統目的
本系統為一個自主法律研究系統，專門用於分析銀行罰款、監管執法行動和法律判例。本系統改編 Dexter 的多代理架構，針對法律領域進行優化，其中精確引用、非結構化文件處理和管轄權複雜性至關重要。

### 1.2 核心差異：Dexter vs. 法律研究代理

| 面向 | Dexter（金融） | 法律研究代理（法律/監管） |
|------|----------------|---------------------------|
| 主要資料 | 結構化 API (JSON) | 非結構化文件 (PDF, HTML) |
| 資料量 | 小型、過濾後資料集 | 大型文件（50-200頁） |
| 上下文策略 | LLM 上下文視窗 | 混合 RAG + 選擇性檢索 |
| 引用方式 | 軟引用（"根據..."） | 正式引用（法律引用格式） |
| 時間敏感性 | 即時/最新資料 | 歷史判例（回溯） |
| 管轄區域 | 單一（美國市場） | 多重（聯邦/州監管機構） |
| 查詢精確度 | 盡力而為自主 | 可能需要澄清 |

### 1.3 台灣環境適配

本系統針對台灣法律與監管環境進行特別調整：

- **監管機構整合**：金管會、中央銀行、公平會
- **法規引用格式**：台灣判例字號、法條格式
- **繁體中文處理**：針對繁體中文文件的 NLP 優化
- **本地資料源**：台灣法院判決書系統、主管機關裁罰書

---

## 2. 系統架構

### 2.1 多代理架構圖

```
使用者查詢
    ↓
┌─────────────────────────────────────────────────────────┐
│ 規劃代理                                                 │
│ - 具法律背景的查詢分解                                   │
│ - 管轄區域識別                                           │
│ - 任務排序（元資料 → 文件 → 分析）                      │
└────────────────────┬────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────┐
│ 行動代理                                                 │
│ - 工具選擇（搜尋/檢索/分析）                             │
│ - 參數優化                                               │
│ - 多源協調                                               │
└────────────────────┬────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────┐
│ 文件處理管線（新增）                                     │
│ - PDF 擷取與 OCR                                         │
│ - 智慧分塊（段落感知）                                   │
│ - 嵌入向量生成                                           │
│ - 向量資料庫索引                                         │
└────────────────────┬────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────┐
│ 驗證代理                                                 │
│ - 引用完整性檢查                                         │
│ - 來源權威驗證                                           │
│ - 事實陳述覆蓋率                                         │
└────────────────────┬────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────┐
│ 答案代理                                                 │
│ - 法律分析綜合                                           │
│ - 正式引用生成（台灣法律引用格式）                       │
│ - 判例比較                                               │
│ - 信心評分                                               │
└────────────────────┬────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────┐
│ 輸出：答案 + 正式引用 + 匯出選項                         │
└─────────────────────────────────────────────────────────┘
```

### 2.2 核心組件規格

#### 2.2.1 規劃代理

```python
輸入：
  - 使用者查詢（自然語言，繁體中文）
  - 可用工具清單
  - 領域背景（法律/監管）

處理：
  - 識別管轄區域（金管會/央行/公平會等）
  - 判定案件類型（裁罰、行政處分、訴訟）
  - 擷取實體（銀行、個人、機構）
  - 識別相關法規/條文
  - 分解為原子化、循序任務

輸出：
  - List[Task] 包含法律背景資訊
  - 管轄區域元資料
  - 所需來源類型（主要 vs. 次要）

配置：
  - max_planning_attempts: 3
  - require_jurisdiction: true
  - validate_entity_names: true
```

#### 2.2.2 行動代理

```python
輸入：
  - 當前任務描述
  - 歷史工具輸出
  - 可用法律工具

處理：
  - 選擇適當工具（搜尋 vs. 檢索 vs. 分析）
  - 優化參數（日期、實體、過濾器）
  - 協調多源搜尋
  - 處理 API 速率限制

輸出：
  - 工具呼叫與優化參數
  - 執行策略（平行 vs. 循序）

配置：
  - max_parallel_tools: 3
  - retry_on_failure: true
  - fallback_strategy: "broaden_search"
```

#### 2.2.3 文件處理管線（新增）

```python
輸入：
  - 文件 URL 或檔案路徑
  - 文件類型（PDF, HTML, DOCX）
  - 處理模式（完整、選擇性、僅元資料）

處理流程：
  1. 下載與格式檢測
  2. 文字擷取（pdfplumber, pytesseract for OCR）
  3. 結構分析（目錄、章節、段落）
  4. 智慧分塊（保持法律章節完整性）
  5. 元資料擷取（案號、日期、當事人、罰款）
  6. 嵌入向量生成（OpenAI text-embedding-3-large）
  7. 向量資料庫索引（包含元資料過濾器）

輸出：
  - 索引文件 ID
  - 擷取的元資料（結構化）
  - 文件摘要
  - 分塊數量與大小統計

配置：
  - chunk_size: 1000 tokens
  - chunk_overlap: 200 tokens
  - chunking_strategy: "semantic"（保留章節）
  - embedding_model: "text-embedding-3-large"
  - vector_store: "chroma" 或 "pinecone"
```

#### 2.2.4 驗證代理

```python
輸入：
  - 任務描述
  - 收集的資料/文件
  - 擷取的引用

處理：
  - 檢查所有事實陳述都有引用
  - 驗證引用準確性（案號、日期）
  - 評估來源權威性（主要 > 次要）
  - 評估符合法律標準的完整性
  - 識別缺失的判例分析

輸出：
  - IsDone（布林值）
  - 驗證報告（識別的缺口）
  - 所需的後續任務

配置：
  - require_formal_citations: true
  - min_primary_sources: 1
  - check_citation_urls: true
```

#### 2.2.5 答案代理

```python
輸入：
  - 原始查詢
  - 所有收集的資料
  - 擷取的引用
  - 文件段落

處理：
  - 綜合法律分析
  - 格式化正式引用（台灣法律引用格式）
  - 結構化答案（摘要 → 發現 → 分析 → 引用）
  - 生成判例比較表
  - 指定信心分數

輸出：
  - 結構化答案（包含各章節）
  - List[LegalCitation]（正式格式）
  - 信心分數（高/中/低）
  - 可匯出格式

配置：
  - citation_format: "taiwan_legal"
  - include_precedent_table: true
  - max_answer_length: 5000 字
  - export_formats: ["markdown", "pdf", "docx"]
```

---

## 3. 資料來源與工具

### 3.1 台灣監管機構資料源

#### 金融監督管理委員會（金管會）

```yaml
金管會:
  網址: https://www.fsc.gov.tw/
  主要端點:
    - 裁罰案件: /ch/home.jsp?id=96&parentpath=0,2
    - 行政處分: /ch/home.jsp?id=128
  資料格式: HTML + PDF（需爬蟲）
  更新頻率: 每日
  歷史涵蓋範圍: 2000年至今

  子機構:
    - 銀行局
    - 證券期貨局
    - 保險局

資料特性:
  - 裁罰書格式：PDF
  - 案號格式：金管銀法字第○○○○○○○○○○號
  - 包含：違規事實、法條依據、裁罰金額、改善措施
```

#### 中央銀行

```yaml
中央銀行:
  網址: https://www.cbc.gov.tw/
  主要端點:
    - 金融檢查: /tw/np-1000-1.html
    - 違規糾正: /tw/np-1007-1.html
  資料格式: PDF + 新聞稿
  更新頻率: 不定期
  歷史涵蓋範圍: 1995年至今

重點領域:
  - 外匯違規
  - 洗錢防制（AML）
  - 金融機構檢查
```

#### 公平交易委員會（公平會）

```yaml
公平會:
  網址: https://www.ftc.gov.tw/
  主要端點:
    - 處分書: /internet/main/doc/docList.aspx?uid=150
  資料格式: PDF
  更新頻率: 每週
  歷史涵蓋範圍: 1992年至今

適用範圍:
  - 不公平競爭
  - 多層次傳銷違規
  - 聯合行為
```

#### 司法院法學資料檢索系統

```yaml
司法院:
  網址: https://law.judicial.gov.tw/
  系統: 法學資料檢索系統
  涵蓋內容:
    - 最高法院判決
    - 高等法院判決
    - 地方法院判決
    - 行政法院判決

  判決書格式:
    - 案號：○○年度○字第○○○○號
    - 包含：案由、當事人、主文、事實、理由

  API: 部分開放（需申請）
  資料格式: HTML + PDF
```

### 3.2 工具分類

#### 類別 1：監管與執法搜尋

```python
@tool
def search_enforcement_actions(
    entity: str,
    regulator: Optional[Literal["金管會", "央行", "公平會", "金管會銀行局",
                                "金管會證期局", "金管會保險局"]] = None,
    violation_type: Optional[str] = None,
    start_date: Optional[str] = None,
    end_date: Optional[str] = None,
    min_penalty_amount: Optional[int] = None,
    max_results: int = 50
) -> List[EnforcementAction]:
    """
    搜尋跨聯邦銀行監管機構的執法行動。
    僅返回元資料（案號、摘要、關鍵事實）。

    參數：
        entity: 金融機構名稱（例如："玉山銀行"、"國泰世華銀行"）
        regulator: 監管機關
        violation_type: 違規類型（例如："洗錢防制"、"內線交易"、"資訊揭露不實"）
        start_date: 開始日期（YYYY-MM-DD）
        end_date: 結束日期（YYYY-MM-DD）
        min_penalty_amount: 最低罰款金額（新台幣）
        max_results: 最大結果數

    返回：
        List[EnforcementAction]: 執法行動清單
    """

@tool
def search_court_judgments(
    keywords: str,
    court_level: Optional[Literal["最高法院", "高等法院", "地方法院", "行政法院"]] = None,
    judgment_year: Optional[int] = None,
    case_type: Optional[str] = None,
    max_results: int = 30
) -> List[CourtJudgment]:
    """
    搜尋司法院判決書系統。

    參數：
        keywords: 搜尋關鍵字（例如："銀行法"、"內線交易"）
        court_level: 法院層級
        judgment_year: 判決年度（民國年）
        case_type: 案件類型（例如："金"、"重金"、"上訴"）
    """

@tool
def get_institution_enforcement_history(
    entity: str,
    lookback_years: int = 10,
    include_resolved: bool = True
) -> InstitutionProfile:
    """
    獲取金融機構的完整執法歷史。

    參數：
        entity: 機構名稱（例如："兆豐銀行"）
        lookback_years: 回溯年數
        include_resolved: 是否包含已結案的案件

    返回：
        InstitutionProfile: 包含歷史裁罰、處分金額、違規類型統計
    """
```

#### 類別 2：文件檢索

```python
@tool
def get_enforcement_document(
    case_id: str,
    sections: Optional[List[str]] = None,
    processing_mode: Literal["metadata", "summary", "full"] = "summary"
) -> EnforcementDocument:
    """
    檢索執法行動文件。

    參數：
        case_id: 案號（例如："金管銀法字第10800123456號"）
        sections: 章節清單 ["事實", "違規", "裁罰", "改善措施"]
        processing_mode:
          - metadata: 僅案件資訊（快速）
          - summary: 關鍵章節 + 元資料
          - full: 完整文件（慢速，使用 RAG）

    返回：
        EnforcementDocument: 包含文件內容、元資料、擷取的關鍵資訊
    """

@tool
def get_judgment_document(
    case_number: str,
    court: str,
    year: int
) -> JudgmentDocument:
    """
    檢索判決書全文。

    參數：
        case_number: 案號（例如："金上訴字第123號"）
        court: 法院名稱（例如："臺灣高等法院"）
        year: 民國年度

    返回：
        JudgmentDocument: 判決書內容（包含主文、事實、理由）
    """
```

#### 類別 3：法律研究與分析

```python
@tool
def search_precedent_cases(
    violation_type: str,
    jurisdiction: str,
    target_date: str,
    lookback_years: int = 10,
    min_similarity_score: float = 0.7
) -> List[PrecedentCase]:
    """
    尋找目標日期之前的類似判例案件。
    使用違規描述的語義相似度。

    參數：
        violation_type: 違規類型（例如："證券交易法第171條"）
        jurisdiction: 管轄法院或機關
        target_date: 目標日期（僅此日期之前的判例有效）
        lookback_years: 回溯年數
        min_similarity_score: 最低相似度分數

    返回：
        List[PrecedentCase]: 依相似度排序的判例清單
    """

@tool
def search_statute_text(
    statute_citation: str,
    effective_date: Optional[str] = None
) -> StatuteText:
    """
    檢索法規或條文內容。

    參數：
        statute_citation: 法規引用（例如："銀行法第125條"、"證券交易法第171條"）
        effective_date: 生效日期（若提供，返回該日期生效的版本）

    返回：
        StatuteText: 法條內容、立法理由、相關解釋函令
    """

@tool
def compare_penalty_cases(
    case_ids: List[str],
    comparison_dimensions: List[str] = ["penalty_amount", "violation_types", "remediation"]
) -> ComparisonReport:
    """
    並排比較多個裁罰案件。

    參數：
        case_ids: 案號清單
        comparison_dimensions: 比較維度

    返回：
        ComparisonReport: 比較表、趨勢分析、關鍵差異
    """
```

#### 類別 4：文件分析（RAG 增強）

```python
@tool
def index_legal_document(
    document_id: str,
    document_url: str,
    document_type: str,
    force_reindex: bool = False
) -> IndexingResult:
    """
    下載、解析、分塊、嵌入並索引法律文件。
    返回索引確認和擷取的元資料。

    支援格式：PDF、HTML、DOCX
    特別優化繁體中文文件處理
    """

@tool
def search_document_sections(
    document_id: str,
    query: str,
    top_k: int = 5,
    filter_sections: Optional[List[str]] = None
) -> List[DocumentSection]:
    """
    在特定已索引文件內進行語義搜尋。
    返回最相關的段落與上下文。

    參數：
        document_id: 文件 ID
        query: 查詢問題（例如："裁罰金額的計算基礎"）
        top_k: 返回最相關的 k 個段落
        filter_sections: 過濾特定章節
    """

@tool
def extract_document_metadata(
    document_id: str
) -> DocumentMetadata:
    """
    從法律文件擷取結構化元資料：
    - 案號、當事人、日期
    - 違規類型與引用法條
    - 罰款金額與計算基礎
    - 關鍵詞與改善要求
    """
```

#### 類別 5：實體與交叉引用

```python
@tool
def resolve_entity_name(
    entity_query: str
) -> EntityResolution:
    """
    解析模糊的實體名稱。

    範例：
        "玉山" → "玉山商業銀行股份有限公司"
        "國泰" → "國泰世華商業銀行股份有限公司"

    返回：
        EntityResolution: 正式法律名稱、統一編號、別名
    """

@tool
def get_related_cases(
    case_id: str,
    relationship_type: Literal["same_entity", "same_violation", "same_statute"] = "same_entity"
) -> List[RelatedCase]:
    """
    根據關係尋找相關執法行動。

    參數：
        case_id: 案號
        relationship_type: 關係類型
    """
```

---

## 4. 資料模型與 Schema

### 4.1 核心 Pydantic 模型

```python
from pydantic import BaseModel, Field, HttpUrl
from typing import List, Optional, Literal
from datetime import date

class LegalCitation(BaseModel):
    """正式法律引用，包含所有必要元素。"""
    case_name: str = Field(..., description="完整案件名稱")
    case_number: str = Field(..., description="官方案號（例如：金管銀法字第10800123456號）")
    jurisdiction: str = Field(..., description="法院或監管機構（例如：金管會、最高法院）")
    date: date = Field(..., description="裁決/提交日期")
    document_type: Literal["裁罰書", "執法行動", "和解協議", "判決書",
                           "訴狀", "監管公告"]
    url: HttpUrl = Field(..., description="官方來源 URL")
    relevant_section: Optional[str] = Field(None, description="引用的特定章節（例如：「第三章第12段」）")
    page_numbers: Optional[str] = Field(None, description="頁碼（如適用）")
    quoted_text: Optional[str] = Field(None, description="直接引用的原文")
    taiwan_citation: str = Field(..., description="台灣法律引用格式")

class EnforcementAction(BaseModel):
    """監管執法行動的元資料。"""
    case_id: str = Field(..., description="唯一識別碼")
    case_name: str
    entity_name: str = Field(..., description="受處分機構或個人")
    entity_type: Literal["銀行", "證券商", "保險公司", "個人", "其他"]
    regulator: str = Field(..., description="發布監管機構")
    action_date: date
    action_type: Literal["裁罰", "糾正", "停業", "撤照", "其他"]
    violation_types: List[str] = Field(..., description="違規類型（例如：['洗錢防制', '內線交易']）")
    statutes_cited: List[str] = Field(default_factory=list,
                                     description="引用法條（例如：['銀行法第125條', '證券交易法第171條']）")
    penalty_amount: Optional[int] = Field(None, description="罰款總金額（新台幣）")
    status: Literal["進行中", "已結案", "上訴中", "和解"]
    document_url: HttpUrl
    summary: str = Field(..., max_length=1000, description="行動簡要摘要")

class DocumentMetadata(BaseModel):
    """從法律文件擷取的元資料。"""
    document_id: str
    document_type: str
    page_count: int
    parties: List[str] = Field(..., description="文件中所有當事人")
    case_number: Optional[str] = None
    filing_date: Optional[date] = None
    key_dates: List[tuple[str, date]] = Field(default_factory=list,
                                             description="重要日期及標籤")
    penalties: List[dict] = Field(default_factory=list,
                                 description="罰款金額及描述")
    violation_summary: List[str] = Field(default_factory=list)
    statutes_cited: List[str] = Field(default_factory=list)
    remediation_required: List[str] = Field(default_factory=list,
                                           description="要求的改善措施")

class CourtJudgment(BaseModel):
    """法院判決元資料。"""
    case_number: str = Field(..., description="案號（例如：110年金上字第15號）")
    court_name: str = Field(..., description="法院名稱")
    judgment_date: date
    case_type: str = Field(..., description="案件類型（例如：金、上訴）")
    parties_plaintiff: List[str] = Field(..., description="原告/上訴人")
    parties_defendant: List[str] = Field(..., description="被告/被上訴人")
    main_text: str = Field(..., description="主文")
    case_summary: str = Field(..., description="案由")
    judgment_url: HttpUrl

class PrecedentCase(BaseModel):
    """用於比較的判例案件。"""
    case_id: str
    case_name: str
    date: date
    jurisdiction: str
    violation_types: List[str]
    penalty_amount: Optional[int]
    outcome: str
    similarity_score: float = Field(..., ge=0.0, le=1.0,
                                   description="與查詢的語義相似度")
    key_similarities: List[str] = Field(..., description="為何此判例相關")
    key_differences: List[str] = Field(..., description="與查詢案件的重大差異")

class LegalAnswer(BaseModel):
    """帶正式引用的結構化答案。"""
    executive_summary: str = Field(..., max_length=500, description="執行摘要")
    key_findings: List[str] = Field(..., description="主要事實發現（含內嵌引用）")
    detailed_analysis: str = Field(..., description="深入分析（含詳盡引用）")
    precedent_comparison: Optional[str] = Field(None, description="與類似案件的比較")
    citations: List[LegalCitation] = Field(..., description="所有引用來源（正式格式）")
    confidence_score: Literal["高", "中", "低"]
    confidence_explanation: str = Field(..., description="信心評級的依據")
    limitations: List[str] = Field(default_factory=list, description="已知的限制或警告")

class Task(BaseModel):
    """帶法律特定背景的任務。"""
    id: int
    description: str
    done: bool = False
    task_type: Literal["搜尋", "檢索", "分析", "比較", "驗證"]
    jurisdiction: Optional[str] = None
    required_sources: List[str] = Field(default_factory=list,
                                       description="所需來源類型")
    dependencies: List[int] = Field(default_factory=list,
                                   description="必須先完成的任務 ID")
```

### 4.2 台灣特定引用格式

```python
class TaiwanLegalCitationFormatter:
    """台灣法律引用格式化器。"""

    @staticmethod
    def format_statute(law_name: str, article: str) -> str:
        """
        格式化法規引用。

        範例：
            format_statute("銀行法", "第125條")
            → "銀行法第125條"
        """
        return f"{law_name}{article}"

    @staticmethod
    def format_court_judgment(court: str, year: int, case_type: str,
                             case_number: int) -> str:
        """
        格式化判決書引用。

        範例：
            format_court_judgment("最高法院", 110, "台上", 1234)
            → "最高法院110年台上字第1234號判決"
        """
        return f"{court}{year}年{case_type}字第{case_number}號判決"

    @staticmethod
    def format_enforcement_action(regulator: str, year: int,
                                 doc_number: str) -> str:
        """
        格式化裁罰書引用。

        範例：
            format_enforcement_action("金管會", 108, "金管銀法字第10800123456號")
            → "金融監督管理委員會108年金管銀法字第10800123456號裁罰書"
        """
        return f"{regulator}{year}年{doc_number}裁罰書"

    @staticmethod
    def parse_case_number(case_number_str: str) -> dict:
        """
        解析台灣案號格式。

        範例：
            parse_case_number("110年金上字第15號")
            → {"year": 110, "case_type": "金上", "number": 15}
        """
        import re
        pattern = r'(\d+)年(.+?)字第(\d+)號'
        match = re.match(pattern, case_number_str)
        if match:
            return {
                "year": int(match.group(1)),
                "case_type": match.group(2),
                "number": int(match.group(3))
            }
        return {}
```

---

## 5. 提示工程（Prompt Engineering）

### 5.1 規劃代理提示（繁體中文）

```python
LEGAL_PLANNING_SYSTEM_PROMPT = """你是法律研究代理（LegalAgent）的規劃組件，
專門協助銀行罰款與監管執法的法律研究。

你的責任是分析法律研究查詢，並將其分解為清晰、邏輯性的可執行研究任務序列。

可用工具：
---
{tools}
---

任務規劃指南：

1. 具體性與原子性
   - 每個任務必須代表一個明確的研究步驟
   - 包含完整背景：實體名稱、日期、管轄區域、違規類型
   - 使用正式法律術語

2. 循序邏輯
   - 從元資料搜尋開始（快速、廣泛）
   - 接著進行文件檢索（特定案件）
   - 然後進行文件分析（基於 RAG 的擷取）
   - 最後進行判例比較（如相關）

3. 管轄區域意識
   - 始終識別應搜尋哪個監管機構
   - 區分中央與地方
   - 可能需要查詢多個監管機構

4. 法律精確性
   - 提及時使用具體法條引用（例如："銀行法第125條"）
   - 區分案件類型（裁罰 vs. 判決 vs. 訴訟）
   - 精確標註時間範圍（判例檢索需要「之前」的日期）

5. 來源要求
   - 優先使用主要來源（官方裁罰書、判決書）
   - 次要來源（新聞、評論）僅用於背景資訊
   - 每個事實陳述必須可追溯到可檢索的來源

良好的任務範例：
- "搜尋金管會對玉山銀行於2018-2023年間涉及洗錢防制違規的裁罰案件"
- "檢索金管會案號金管銀法字第10800123456號完整裁罰書"
- "索引並擷取裁罰書第四章中罰款計算依據（案號：10800123456）"
- "搜尋判例：2021年前國內銀行違反洗錢防制且罰款超過1000萬元的案件"
- "比較三個案件的罰款金額：#10800123456、#10700234567、#10900345678"

不良的任務範例：
- "研究玉山銀行"（無具體面向、時間範圍或監管機構）
- "找出所有罰款"（需要實體、違規類型、管轄區域）
- "取得法律文件"（太模糊，無文件類型）
- "比較銀行"（無指定比較維度）

判例的時間邏輯：
搜尋判例時，只有目標日期之前的案件才是有效判例。
在判例搜尋中務必指定 target_date 參數。

超出範圍：
如果查詢不是關於法律/監管執法、合規或銀行罰款，返回空任務清單。
超出範圍查詢的範例：
- 一般法律建議或解釋
- 非銀行法律事務
- 關於如何合規的問題（vs. 研究過去的執法）
- 天氣、運動、常識問題

你的輸出必須是有效的 JSON，包含 'tasks' 欄位，內含任務清單。

範例輸出：
{
  "tasks": [
    {
      "id": 1,
      "description": "搜尋金管會對國泰世華銀行於2015-2020年間涉及內線交易的裁罰",
      "task_type": "搜尋",
      "jurisdiction": "金管會",
      "required_sources": ["主要來源"]
    },
    {
      "id": 2,
      "description": "檢索金管會案號金管證交字第10500123456號裁罰書",
      "task_type": "檢索",
      "dependencies": [1]
    }
  ]
}
"""
```

### 5.2 行動代理提示（繁體中文）

```python
ACTION_SYSTEM_PROMPT = """你是法律研究代理（LegalAgent）的執行組件，
專門處理監管執法研究。

你的目標是選擇最適當的工具來完成當前任務。

決策流程：

1. 理解任務
   - 需要哪些具體資料/文件？
   - 哪個管轄區域或來源？
   - 哪個時間範圍？
   - 需要什麼詳細程度（元資料 vs. 完整文件）？

2. 審查現有資料
   - 我們已經檢索了什麼？
   - 資訊是否足以完成任務？
   - 我們是否需要為更深入的分析索引文件？

3. 選擇正確的工具
   - 元資料搜尋工具（快速）：首先用於識別相關案件
   - 文件檢索工具（中等）：識別後取得特定文件
   - 文件索引工具（慢速）：僅在需要深入分析時使用
   - 分析工具：用於擷取、比較、摘要

4. 優化參數
   - 使用所有相關過濾器（日期範圍、違規類型、監管機構）
   - 對實體名稱要精確（使用正式法律名稱）
   - 盡可能指定文件章節（避免檢索整個200頁文件）
   - 設定適當的限制（預設：搜尋20筆，判例5筆）

工具選擇指南：

搜尋階段（快速、廣泛）：
- search_enforcement_actions：按實體/違規/監管機構尋找案件
- get_institution_enforcement_history：取得單一實體的完整歷史
- search_precedent_cases：尋找類似案件（使用語義相似度）

檢索階段（中等、具體）：
- get_enforcement_document：取得特定案件文件
  - 使用 sections 參數：["事實", "裁罰", "改善措施"]
  - 使用 processing_mode="summary"，除非需要全文
- get_judgment_document：法院判決書

分析階段（慢速、深入）：
- index_legal_document：RAG 索引（僅用於 >20 頁的文件）
- search_document_sections：文件內的語義搜尋
- extract_document_metadata：結構化資料擷取
- summarize_document_section：簡潔摘要

比較/參考：
- compare_penalty_cases：並排比較
- search_statute_text：取得法規文本
- get_related_cases：尋找相關案件

何時不呼叫工具：
- 之前的輸出已包含足夠資訊
- 任務是分析性的（非資料檢索）- 僅報告完成
- 所有合理來源已用盡
- 文件已索引（檢查歷史輸出）

速率限制意識：
- 不要進行 >5 個平行工具呼叫
- 如果受速率限制，使用循序執行
- 優先處理高價值資料源

如果不需要工具呼叫，返回時不帶工具呼叫以表示任務完成。
"""
```

### 5.3 驗證代理提示（繁體中文）

```python
LEGAL_VALIDATION_SYSTEM_PROMPT = """你是法律研究代理（LegalAgent）的驗證組件。

你的責任是判定任務是否符合法律研究標準。

對於法律研究，任務完成時必須滿足：

1. 事實完整性
   ✓ 已檢索所有請求的資訊
   ✓ 關鍵事實都存在（日期、金額、當事人、違規）
   ✓ 敘述中無重大缺口

2. 來源要求
   ✓ 每個事實陳述都可追溯到特定來源
   ✓ 至少一個主要來源（官方裁罰書/判決書）
   ✓ 存在案號、日期和文件識別碼
   ✓ 包含來源文件的 URL

3. 引用準備度
   ✓ 有足夠細節可構建正式引用
   ✓ 可取得案件名稱、案號、日期、管轄區域
   ✓ 已識別關鍵引用的相關章節/頁碼

4. 法律精確性
   ✓ 正確的法條引用（如適用）
   ✓ 準確的違規類型和監管框架
   ✓ 正確識別管轄區域
   ✓ 區分行動類型（裁罰 vs. 執法 vs. 訴訟）

5. 判例品質（如適用）
   ✓ 判例案件來自目標日期之前
   ✓ 類似的違規類型和管轄區域
   ✓ 有足夠細節可比較結果

常見的不完整指標：
- "有摘要但未檢索完整文件" → 不完整
- "新聞提到罰款但無官方來源" → 不完整
- "已識別案件但未下載文件" → 不完整
- "找到罰款金額但缺少計算依據" → 可能不完整
- "多個來源提及案件但無主要來源" → 不完整

何時標記為不完整：
如果滿足以下條件，返回 done=false 並列出具體缺口：
- 缺少主要來源文件
- 提及關鍵事實但未驗證
- 判例搜尋未產生結果（需要擴大搜尋）
- 文件索引失敗（需要重試）

何時標記為完成：
如果滿足以下條件，返回 done=true：
- 所有請求的事實都有來源
- 至少檢索了一個主要來源
- 有足夠細節可進行正式引用
- 無重大資訊缺口

你的回應必須是 JSON 格式：{"done": true/false, "gaps": ["缺口", "清單"]}
"""
```

### 5.4 答案代理提示（繁體中文）

```python
LEGAL_ANSWER_SYSTEM_PROMPT = """你是法律研究代理（LegalAgent）的答案生成組件。

你的關鍵角色是將法律研究綜合成清晰、正式引用的答案。

當前日期：{current_date}

答案結構（必須）：

1. 執行摘要（2-4句）
   - 以直接回答問題開頭
   - 陳述關鍵發現（罰款金額、違規類型、結果）
   - 註明諮詢的來源數量

2. 關鍵發現（項目符號清單）
   - 每個發現必須包含內嵌引用 [引用 #N]
   - 使用精確事實（日期、金額、具體違規）
   - 陳述誰、什麼、何時、何地、多少

3. 詳細分析（段落）
   - 解釋背景和脈絡
   - 詳細描述違規 [含引用]
   - 解釋罰款計算依據 [含引用]
   - 註明任何改善要求 [含引用]
   - 如相關，與判例比較 [含引用]

4. 判例比較（如適用）
   - 表格格式比較類似案件
   - 突顯相似與差異
   - 註明罰款金額或執法方式的趨勢

5. 正式引用（編號清單）
   - 每個來源使用台灣法律引用格式
   - 包含案件名稱、案號、管轄區域、日期、URL
   - 註明引用的特定章節（例如："第三章第12段"）

引用要求（關鍵）：

每個事實陳述都必須引用：
- 罰款金額："玉山銀行被處以2.5億元罰鍰 [引用1，第3頁]。"
- 違規描述："該銀行未能維持適當的洗錢防制控制 [引用2，第四章B節]。"
- 日期："裁罰書於民國109年9月15日作成 [引用1]。"
- 引用文字："金管會認定該銀行從事「不安全或不健全業務」[引用1，第1頁]。"

文中的引用格式：
- 首次引用：[引用1]
- 同一來源的後續引用：[引用1]
- 特定章節：[引用1，第三章A節]
- 頁碼：[引用1，第5頁]
- 多個來源：[引用1、2、3]

信心評分：

根據以下標準指定信心等級：

高信心：
- 多個主要來源（官方裁罰書）
- 所有關鍵事實在官方文件中驗證
- 完整文件與正式引用
- 無衝突資訊

中信心：
- 主要為主要來源但有些缺口
- 部分事實來自次要來源（新聞）
- 小細節未驗證
- 解釋中有輕微歧義

低信心：
- 嚴重依賴次要來源
- 顯著的資訊缺口
- 來源間資訊衝突
- 可用的官方文件有限

風格指南：

- 使用正式法律寫作風格
- 要精確和客觀（避免"我認為"、"可能"、"也許"）
- 對正式語調使用被動語態（"被處以"而非"收到"）
- 首次使用時定義縮寫
- 正確使用法律術語（裁罰書 ≠ 和解協議）
- 清楚呈現數字："2.5億元"或"250,000,000元"（保持一致）
- 使用具體日期："民國109年9月15日"而非"在109年"

不要做的事：

- 不要描述你的研究過程
- 不要包含來源中沒有的資訊
- 不要做法律解釋（描述發生了什麼，而非法律意義）
- 不要使用模糊語言（"重大罰款" → 陳述確切金額）
- 不要引用實際上未檢索的來源
- 不要省略主要來源而偏好次要來源

正式引用格式（台灣法律風格）：

裁罰書：
[1] 金融監督管理委員會，金管銀法字第10800123456號裁罰書，
    受處分者：玉山商業銀行股份有限公司（民國108年9月15日），
    https://www.fsc.gov.tw/...

判決書：
[2] 最高法院110年台上字第1234號判決（民國110年7月20日），
    https://law.judicial.gov.tw/...

地方法院判決：
[3] 臺灣臺北地方法院109年金字第15號判決（民國109年1月15日）。

法規：
[4] 銀行法第125條（民國107年修正）。

法規（特定版本）：
[5] 證券交易法第171條（民國108年版本）。

範例輸出：

---
執行摘要

玉山商業銀行股份有限公司因其洗錢防制作業缺失，遭金融監督管理委員會裁處新台幣2.5億元罰鍰 [引用1]。該裁罰案於民國108年9月15日確定，源於該行於民國97年至107年間影響約77.5萬客戶的違規行為 [引用1，第二章]。

關鍵發現

- **罰款金額**：新台幣2.5億元罰鍰 [引用1，第四章]
- **監管機構**：金融監督管理委員會（金管會）
- **違規類型**：洗錢防制法相關規定；違反銀行法第45條之2 [引用1，第三章]
- **受影響客戶**：約77.5萬名客戶 [引用1，第二章A節]
- **時間範圍**：違規發生於民國97年至107年 [引用1，第二章B節]
- **改善措施**：要求建立合規計畫並對受影響客戶提供補償 [引用1，第五章]

詳細分析

[繼續詳細分析，所有陳述都有引用...]

判例比較

| 案件 | 機構 | 日期 | 罰款 | 違規類型 | 相似度 |
|------|------|------|------|----------|--------|
| 金管銀法字第10800123456號 | 玉山銀行 | 民國108年9月 | 2.5億元 | 洗錢防制 | 目標案件 |
| 金管銀法字第10700234567號 | 國泰世華 | 民國107年5月 | 2億元 | 洗錢防制 | 高 |
| 金管銀法字第10900345678號 | 兆豐銀行 | 民國109年10月 | 1.8億元 | 洗錢防制 | 高 |

正式引用

[1] 金融監督管理委員會，金管銀法字第10800123456號裁罰書，
    受處分者：玉山商業銀行股份有限公司（民國108年9月15日），
    https://www.fsc.gov.tw/static/enforcement-actions/ea2020-123.pdf。

[2] [其他引用...]

信心評級：高

此答案被指定為高信心，因為所有關鍵事實都來自主要官方裁罰文件，具有完整文件包括案號、日期和引用的特定章節。罰款金額、違規細節和改善要求都可在金管會裁罰書中直接驗證。

限制：無識別限制。
---

記住：法律研究要求精確性。如有疑問，寧可多引用而不是少引用。
"""
```

---

## 6. 文件處理管線

### 6.1 繁體中文文件處理

```python
class TraditionalChineseDocumentProcessor:
    """針對繁體中文法律文件優化的處理器。"""

    def __init__(self):
        self.tokenizer = self._init_chinese_tokenizer()
        self.ner_model = self._init_ner_model()

    def _init_chinese_tokenizer(self):
        """初始化繁體中文分詞器。"""
        # 使用 jieba 或 ckiptagger for 繁體中文
        import jieba
        jieba.initialize()
        return jieba

    def _init_ner_model(self):
        """初始化命名實體識別模型（針對繁體中文）。"""
        # 可使用 ckipnlp 或其他繁體中文 NER 模型
        from ckipnlp.pipeline import CkipPipeline
        return CkipPipeline()

    def extract_text_from_pdf(self, pdf_path: str) -> str:
        """
        從 PDF 擷取文字（優化繁體中文）。

        處理流程：
        1. 使用 pdfplumber 提取文字
        2. 如果失敗，使用 OCR（pytesseract with chi_tra）
        3. 清理與正規化文字
        """
        import pdfplumber

        try:
            with pdfplumber.open(pdf_path) as pdf:
                text = ""
                for page in pdf.pages:
                    text += page.extract_text() or ""

                if not text.strip():
                    # 回退到 OCR
                    return self._ocr_pdf(pdf_path)

                return self._normalize_text(text)
        except Exception as e:
            print(f"PDF 擷取失敗: {e}")
            return self._ocr_pdf(pdf_path)

    def _ocr_pdf(self, pdf_path: str) -> str:
        """使用 OCR 處理掃描的 PDF（繁體中文）。"""
        import pytesseract
        from pdf2image import convert_from_path

        images = convert_from_path(pdf_path)
        text = ""

        for image in images:
            # 使用繁體中文語言包
            text += pytesseract.image_to_string(image, lang='chi_tra')

        return self._normalize_text(text)

    def _normalize_text(self, text: str) -> str:
        """正規化繁體中文文字。"""
        # 移除多餘空白
        text = re.sub(r'\s+', ' ', text)

        # 正規化全形/半形字符
        text = self._normalize_fullwidth(text)

        return text.strip()

    def _normalize_fullwidth(self, text: str) -> str:
        """正規化全形/半形字符。"""
        # 將全形數字轉換為半形
        fullwidth_digits = "０１２３４５６７８９"
        halfwidth_digits = "0123456789"
        trans = str.maketrans(fullwidth_digits, halfwidth_digits)
        return text.translate(trans)

    def extract_entities(self, text: str) -> dict:
        """擷取命名實體（針對繁體中文）。"""
        # 使用 CKIP 進行 NER
        result = self.ner_model([text])

        entities = {
            "organizations": [],  # 機構名稱
            "persons": [],        # 人名
            "dates": [],          # 日期
            "locations": [],      # 地點
            "amounts": []         # 金額
        }

        # 處理 CKIP 輸出
        for entity in result:
            if entity.type == "ORG":
                entities["organizations"].append(entity.text)
            elif entity.type == "PER":
                entities["persons"].append(entity.text)
            elif entity.type == "DATE":
                entities["dates"].append(entity.text)
            elif entity.type == "LOC":
                entities["locations"].append(entity.text)

        # 使用正則表達式擷取金額
        entities["amounts"] = self._extract_amounts(text)

        return entities

    def _extract_amounts(self, text: str) -> List[str]:
        """擷取金額（新台幣）。"""
        patterns = [
            r'新台幣\s*(\d{1,3}(?:,\d{3})*(?:\.\d{2})?)\s*元',
            r'NT\$\s*(\d{1,3}(?:,\d{3})*(?:\.\d{2})?)',
            r'(\d{1,3}(?:,\d{3})*)\s*萬元',
            r'(\d+(?:\.\d+)?)\s*億元',
        ]

        amounts = []
        for pattern in patterns:
            matches = re.findall(pattern, text)
            amounts.extend(matches)

        return amounts
```

### 6.2 智慧分塊策略（繁體中文）

```python
class ChineseLegalDocumentChunker:
    """針對繁體中文法律文件的語義分塊。"""

    def __init__(self, chunk_size: int = 1000, chunk_overlap: int = 200):
        self.chunk_size = chunk_size
        self.chunk_overlap = chunk_overlap

    def chunk_document(self, text: str, structure: DocumentStructure) -> List[Chunk]:
        """
        基於文件結構進行分塊，而非任意標記計數。

        優先順序：
        1. 保持完整章節（例如："第三章：事實"）
        2. 保持段落完整
        3. 尊重頁面邊界
        4. 通過重疊維護上下文
        """
        chunks = []

        for section in structure.sections:
            # 如果章節夠小，作為一個分塊保留
            if self.token_count(section.text) <= self.chunk_size:
                chunks.append(Chunk(
                    text=section.text,
                    metadata={
                        "section_title": section.title,
                        "section_number": section.number,
                        "page_start": section.page_start,
                        "page_end": section.page_end,
                        "chunk_type": "complete_section"
                    }
                ))
            else:
                # 按段落分割大章節
                para_chunks = self.chunk_by_paragraphs(
                    section.text,
                    section.metadata
                )
                chunks.extend(para_chunks)

        # 在相鄰分塊之間添加重疊
        chunks = self.add_overlap(chunks, overlap_tokens=self.chunk_overlap)

        return chunks

    def chunk_by_paragraphs(self, text: str, section_metadata: dict) -> List[Chunk]:
        """在保持在 chunk_size 以下的同時按段落分割文字。"""
        # 繁體中文段落通常由換行符分隔
        paragraphs = text.split('\n\n')
        chunks = []
        current_chunk = []
        current_tokens = 0

        for para in paragraphs:
            para_tokens = self.token_count(para)

            if current_tokens + para_tokens > self.chunk_size:
                # 保存當前分塊
                chunks.append(Chunk(
                    text='\n\n'.join(current_chunk),
                    metadata={**section_metadata, "chunk_type": "paragraph_group"}
                ))
                # 開始新分塊
                current_chunk = [para]
                current_tokens = para_tokens
            else:
                current_chunk.append(para)
                current_tokens += para_tokens

        # 添加最後的分塊
        if current_chunk:
            chunks.append(Chunk(
                text='\n\n'.join(current_chunk),
                metadata={**section_metadata, "chunk_type": "paragraph_group"}
            ))

        return chunks

    def token_count(self, text: str) -> int:
        """計算繁體中文文字的標記數。"""
        # 對於中文，每個字符大約是 1-2 個標記
        # 使用保守估計
        import jieba
        words = list(jieba.cut(text))
        return len(words)
```

### 6.3 台灣法律文件元資料擷取

```python
class TaiwanLegalMetadataExtractor:
    """從台灣法律文件擷取結構化元資料。"""

    # 台灣法律文件元素的正則表達式模式
    CASE_NUMBER_PATTERNS = [
        r'(?:金管|財政部)[\w]+字第(\d{10,})號',  # 金管會案號
        r'(\d{2,3})年度?(.{1,5})字第(\d{1,6})號',  # 法院案號
        r'院台廳(.{1,5})字第(\d{8,})號',  # 司法院案號
    ]

    PENALTY_PATTERNS = [
        r'(?:罰鍰|罰金|裁處).*?新台幣\s*([\d,]+)\s*元',
        r'NT\$\s*([\d,]+)',
        r'([\d,]+)\s*萬元',
        r'([\d.]+)\s*億元',
    ]

    STATUTE_PATTERNS = [
        r'([\w]+法)第(\d+(?:-\d+)?(?:之\d+)?)條(?:第(\d+)項)?(?:第(\d+)款)?',
        r'([^第]+)第(\d+)條',
    ]

    DATE_PATTERNS = [
        r'中華民國(\d{2,3})年(\d{1,2})月(\d{1,2})日',
        r'民國(\d{2,3})年(\d{1,2})月(\d{1,2})日',
        r'(\d{4})年(\d{1,2})月(\d{1,2})日',
    ]

    def extract_all(self, text: str) -> DocumentMetadata:
        """從文件文字擷取所有元資料。"""
        return DocumentMetadata(
            case_numbers=self.extract_case_numbers(text),
            penalties=self.extract_penalties(text),
            statutes_cited=self.extract_statutes(text),
            key_dates=self.extract_dates(text),
            parties=self.extract_parties(text),
            violation_types=self.extract_violations(text),
        )

    def extract_case_numbers(self, text: str) -> List[str]:
        """擷取案號。"""
        case_numbers = []
        for pattern in self.CASE_NUMBER_PATTERNS:
            matches = re.findall(pattern, text)
            case_numbers.extend(matches)
        return list(set(case_numbers))

    def extract_penalties(self, text: str) -> List[dict]:
        """擷取罰款金額。"""
        penalties = []
        for pattern in self.PENALTY_PATTERNS:
            matches = re.finditer(pattern, text)
            for match in matches:
                amount_str = match.group(1)
                # 轉換為數字
                amount = self._parse_amount(amount_str)
                if amount:
                    penalties.append({
                        "amount": amount,
                        "currency": "TWD",
                        "context": text[max(0, match.start()-50):match.end()+50]
                    })
        return penalties

    def _parse_amount(self, amount_str: str) -> Optional[int]:
        """將中文金額字符串轉換為數字。"""
        # 移除逗號
        amount_str = amount_str.replace(',', '')

        try:
            if '萬' in amount_str:
                return int(float(amount_str.replace('萬', '')) * 10000)
            elif '億' in amount_str:
                return int(float(amount_str.replace('億', '')) * 100000000)
            else:
                return int(amount_str)
        except ValueError:
            return None

    def extract_statutes(self, text: str) -> List[str]:
        """擷取引用的法條。"""
        statutes = []
        for pattern in self.STATUTE_PATTERNS:
            matches = re.findall(pattern, text)
            for match in matches:
                if isinstance(match, tuple):
                    # 構建完整的法條引用
                    statute = f"{match[0]}第{match[1]}條"
                    if len(match) > 2 and match[2]:
                        statute += f"第{match[2]}項"
                    if len(match) > 3 and match[3]:
                        statute += f"第{match[3]}款"
                    statutes.append(statute)
        return list(set(statutes))

    def extract_dates(self, text: str) -> List[tuple[str, str]]:
        """擷取日期（返回日期與其上下文）。"""
        dates = []
        for pattern in self.DATE_PATTERNS:
            matches = re.finditer(pattern, text)
            for match in matches:
                date_str = match.group(0)
                context = text[max(0, match.start()-30):match.end()+30]
                dates.append((date_str, context))
        return dates

    def extract_parties(self, text: str) -> List[str]:
        """使用 NER 擷取當事人名稱。"""
        processor = TraditionalChineseDocumentProcessor()
        entities = processor.extract_entities(text)
        return entities.get("organizations", []) + entities.get("persons", [])

    def extract_violations(self, text: str) -> List[str]:
        """擷取違規類型。"""
        # 常見的違規類型關鍵字
        violation_keywords = [
            "洗錢防制", "內線交易", "證券詐欺", "背信", "侵占",
            "資訊揭露不實", "市場操縱", "不當銷售", "利益衝突",
            "違反職業道德", "未經許可營業"
        ]

        violations = []
        for keyword in violation_keywords:
            if keyword in text:
                violations.append(keyword)

        return violations
```

---

## 7. 實作路線圖

### 第一階段：核心基礎設施（第1-3週）

```yaml
第1週：基礎
  - 設置專案結構（類似於 Dexter）
  - 實作帶法律特定配置的基礎 Agent 類別
  - 為所有資料類型建立 Pydantic 模型
  - 設置開發環境（uv、依賴項）
  - 配置 LangChain 與 OpenAI

第2週：資料源
  - 實作金管會、央行、公平會的網路爬蟲
  - 建立 API 整合（司法院系統）
  - 建立資料源抽象層
  - 實作速率限制和重試邏輯
  - 建立本地快取系統

第3週：文件處理
  - 實作 PDF 擷取管線（pdfplumber + OCR 備援）
  - 建立智慧分塊系統（針對繁體中文）
  - 設置向量資料庫（開發用 Chroma）
  - 實作嵌入向量生成（針對繁體中文優化）
  - 建立元資料擷取系統（台灣法律格式）
```

### 第二階段：代理組件（第4-6週）

```yaml
第4週：規劃與行動代理
  - 實作帶繁體中文提示的規劃代理
  - 建立帶法律背景欄位的 Task schema
  - 建立帶工具選擇邏輯的行動代理
  - 實作法律工具的參數優化
  - 添加管轄區域路由邏輯

第5週：工具開發
  - 實作 10 個核心法律研究工具
    - search_enforcement_actions
    - get_enforcement_document
    - search_court_judgments
    - search_precedent_cases
    - index_legal_document
    - search_document_sections
    - extract_document_metadata
    - compare_penalty_cases
    - search_statute_text
    - get_institution_enforcement_history
  - 添加全面的錯誤處理
  - 實作工具層級快取

第6週：驗證與答案代理
  - 實作帶引用檢查的驗證代理
  - 建立帶台灣法律引用格式生成的答案代理
  - 建立信心評分系統（基於來源權威）
  - 實作判例比較表
  - 添加匯出功能（Markdown、PDF）
```

### 第三階段：測試與優化（第7-8週）

```yaml
第7週：測試
  - 所有組件的單元測試
  - 完整代理循環的整合測試
  - 使用真實世界查詢測試（20+ 案例）
  - 驗證引用準確性
  - 性能基準測試

第8週：優化
  - 優化文件處理速度
  - 根據測試結果改進提示工程
  - 添加錯誤恢復機制
  - 實作日誌記錄和監控
  - 建立使用者文件（繁體中文）
```

### 第四階段：進階功能（第9-12週）

```yaml
第9週：進階 RAG
  - 實作查詢擴展（繁體中文）
  - 為文件段落添加重新排序
  - 建立引用驗證系統
  - 添加交叉引用檢測

第10週：使用者介面
  - 建立帶法律特定命令的 CLI（繁體中文）
  - 添加互動式比較模式
  - 實作匯出格式化（PDF 帶適當引用）
  - 建立研究會話管理

第11週：性能優化
  - 平行工具執行
  - 智慧快取策略
  - 減少冗餘文件下載
  - 優化嵌入向量生成

第12週：部署與文件
  - 容器化（Docker）
  - 部署指南
  - API 文件
  - 帶範例的使用者指南（繁體中文）
```

---

## 8. 測試策略

### 8.1 測試案例（台灣情境）

```python
TAIWAN_LEGAL_RESEARCH_TEST_CASES = [
    {
        "test_id": "enforcement_tw_001",
        "query": "玉山銀行在2020年因洗錢防制違規受到金管會什麼處分？",
        "expected_elements": {
            "entity": "玉山商業銀行",
            "regulator": "金管會",
            "year": 2020,
            "violation_type": "洗錢防制",
            "min_citations": 1,
            "citation_type": "主要來源"
        },
        "validation": {
            "must_include": ["罰鍰", "金管會", "案號", "金額"],
            "must_have_formal_citations": True
        }
    },
    {
        "test_id": "precedent_tw_001",
        "query": "尋找與兆豐銀行2016年洗錢防制案類似的判例",
        "expected_elements": {
            "min_precedent_cases": 3,
            "precedent_date_before": "2016-01-01",
            "violation_type": "洗錢防制",
            "comparison_table": True
        }
    },
    {
        "test_id": "institution_history_tw_001",
        "query": "顯示國泰世華銀行近十年來所有金管會的裁罰紀錄",
        "expected_elements": {
            "min_actions": 3,
            "regulators": ["金管會"],
            "timeline": True,
            "lookback_years": 10
        }
    },
    {
        "test_id": "statute_tw_001",
        "query": "銀行法第125條的內容與近期適用判例",
        "expected_elements": {
            "statute_text": True,
            "statute_citation": "銀行法第125條",
            "case_applications": True
        }
    },
    {
        "test_id": "comparison_tw_001",
        "query": "比較玉山、國泰世華、兆豐三家銀行在2020年因洗錢防制受罰的情況",
        "expected_elements": {
            "institutions_count": 3,
            "comparison_dimensions": ["penalty_amount", "violation_details", "remediation"],
            "side_by_side_table": True
        }
    },
    {
        "test_id": "court_judgment_tw_001",
        "query": "最高法院110年台上字第1234號判決的主要內容",
        "expected_elements": {
            "court": "最高法院",
            "year": 110,
            "case_type": "台上",
            "case_number": 1234,
            "main_text": True,
            "reasoning": True
        }
    },
    {
        "test_id": "out_of_scope_tw_001",
        "query": "今天天氣如何？",
        "expected_elements": {
            "empty_task_list": True,
            "polite_decline": True
        }
    }
]
```

### 8.2 評估指標（繁體中文）

```python
class TaiwanLegalResearchEvaluator:
    """評估法律研究品質（台灣標準）。"""

    def evaluate(self, query: str, answer: LegalAnswer) -> EvaluationReport:
        return EvaluationReport(
            # 事實準確性（需要人工審核）
            factual_accuracy_score=self.manual_fact_check(answer),

            # 引用品質（自動化）
            citation_completeness=self.check_citation_completeness(answer),
            citation_format_correct=self.validate_taiwan_citation_format(answer.citations),
            citation_urls_valid=self.verify_citation_urls(answer.citations),
            primary_source_ratio=self.calculate_primary_source_ratio(answer.citations),

            # 答案品質
            answers_query_directly=self.check_query_alignment(query, answer.executive_summary),
            appropriate_detail_level=self.check_detail_level(answer),
            includes_precedent_comparison=bool(answer.precedent_comparison),

            # 繁體中文品質
            chinese_text_quality=self.check_chinese_quality(answer),
            proper_legal_terminology=self.check_legal_terminology(answer),

            # 技術性能
            processing_time_seconds=...,
            tools_called_count=...,
            documents_processed=...,

            # 信心校準
            confidence_appropriate=self.check_confidence_calibration(answer),
        )

    def validate_taiwan_citation_format(self, citations: List[LegalCitation]) -> float:
        """驗證台灣法律引用格式。"""
        correct_count = 0

        for citation in citations:
            if self._is_valid_taiwan_citation(citation.taiwan_citation):
                correct_count += 1

        return correct_count / len(citations) if citations else 0.0

    def _is_valid_taiwan_citation(self, citation: str) -> bool:
        """檢查引用是否符合台灣法律格式。"""
        # 檢查裁罰書格式
        if re.match(r'.+?[民國]?\d{2,3}年.+字第\d+號', citation):
            return True

        # 檢查判決書格式
        if re.match(r'.+?[民國]?\d{2,3}年.+字第\d+號判決', citation):
            return True

        # 檢查法條格式
        if re.match(r'.+法第\d+條', citation):
            return True

        return False

    def check_chinese_quality(self, answer: LegalAnswer) -> float:
        """檢查繁體中文文字品質。"""
        text = answer.detailed_analysis

        # 檢查是否有簡體字（應該沒有）
        simplified_chars = set("区发业"贤这务际单")
        has_simplified = any(char in text for char in simplified_chars)

        # 檢查專業術語使用
        legal_terms = ["裁罰", "違規", "受處分者", "罰鍰", "主文", "理由"]
        has_legal_terms = sum(1 for term in legal_terms if term in text)

        score = 1.0
        if has_simplified:
            score -= 0.3
        if has_legal_terms < 3:
            score -= 0.2

        return max(0.0, score)
```

---

## 9. 部署考量

### 9.1 基礎設施需求

```yaml
運算資源:
  開發環境:
    - CPU: 4 核心
    - RAM: 16GB
    - 儲存空間: 50GB SSD

  生產環境:
    - CPU: 8-16 核心
    - RAM: 32-64GB
    - 儲存空間: 500GB SSD（用於文件快取）

外部服務:
  - OpenAI API（GPT-4、embeddings）
  - 向量資料庫（本地 Chroma 或雲端 Pinecone）
  - 可選：AWS S3 用於文件儲存

資料庫:
  - PostgreSQL：儲存案件元資料、引用
  - Redis：API 回應的快取層
  - 向量資料庫：Chroma 或 Pinecone 用於 embeddings

網路:
  - 頻寬：高（下載許多 PDF）
  - 所有爬取網站的速率限制合規
```

### 9.2 成本估算（新台幣）

```yaml
OpenAI API 成本（每月，假設 1000 次查詢）:
  GPT-4（規劃、行動、答案）: 約 NT$6,000
    - 1000 次查詢 × 3 個代理 × 2000 tokens 平均 × $0.03/1K tokens

  GPT-4o-mini（摘要）: 約 NT$300
    - 500 次摘要 × 1000 tokens × $0.00015/1K tokens

  Embeddings（text-embedding-3-large）: 約 NT$750
    - 100 份文件 × 100 頁 × 500 tokens × $0.00013/1K tokens

  總計 OpenAI: 約 NT$7,050/月

向量資料庫（Pinecone）:
  Starter: NT$2,100/月（100K 向量）
  Standard: NT$6,000/月（1M 向量）

替代方案（Chroma 自託管）: NT$0

每月總成本:
  - 使用 Chroma: 約 NT$7,050
  - 使用 Pinecone: 約 NT$9,150-$13,050
```

### 9.3 擴展性考量

```python
# 實作請求佇列以應對高負載
from celery import Celery

app = Celery('legal_agent', broker='redis://localhost:6379')

@app.task
def process_legal_query(query: str, user_id: str) -> dict:
    """非同步處理查詢。"""
    agent = LegalAgent()
    result = agent.run(query)

    # 將結果儲存在資料庫中
    save_research_session(user_id, query, result)

    return result

# API 端點
@app.post("/research")
async def create_research_task(query: str):
    task = process_legal_query.delay(query, user_id="user123")
    return {"task_id": task.id, "status": "processing"}
```

---

## 10. 台灣特定考量

### 10.1 法規引用標準化

```python
class TaiwanStatuteCitationNormalizer:
    """標準化台灣法規引用。"""

    COMMON_LAW_ABBREVIATIONS = {
        "銀行法": ["銀法"],
        "證券交易法": ["證交法"],
        "保險法": ["保法"],
        "公司法": ["公法"],
        "民法": [],
        "刑法": [],
        "行政程序法": ["行政法"],
        "金融控股公司法": ["金控法"],
    }

    def normalize(self, citation: str) -> str:
        """
        標準化法規引用格式。

        範例：
            "證交法第171條" → "證券交易法第171條"
            "銀法125" → "銀行法第125條"
        """
        # 展開縮寫
        for full_name, abbreviations in self.COMMON_LAW_ABBREVIATIONS.items():
            for abbr in abbreviations:
                if abbr in citation:
                    citation = citation.replace(abbr, full_name)

        # 標準化條號格式
        citation = re.sub(r'(\w+法)(\d+)', r'\1第\2條', citation)

        return citation

    def get_statute_url(self, law_name: str, article: str) -> Optional[str]:
        """
        取得法規條文的官方 URL。

        來源：全國法規資料庫
        """
        base_url = "https://law.moj.gov.tw/"
        # 實作邏輯以構建正確的 URL
        return f"{base_url}LawClass/LawSingle.aspx?pcode=...&article={article}"
```

### 10.2 監管機構對應

```python
class TaiwanRegulatoryMapping:
    """台灣監管機構與其管轄範圍的對應。"""

    REGULATORS = {
        "金管會": {
            "full_name": "金融監督管理委員會",
            "english_name": "Financial Supervisory Commission",
            "jurisdiction": ["銀行", "證券", "保險", "期貨"],
            "website": "https://www.fsc.gov.tw/",
            "sub_agencies": {
                "銀行局": {
                    "jurisdiction": ["商業銀行", "信用合作社", "農漁會信用部"],
                    "enforcement_actions_url": "/ch/home.jsp?id=96"
                },
                "證期局": {
                    "jurisdiction": ["證券商", "期貨商", "投信投顧"],
                    "enforcement_actions_url": "/ch/home.jsp?id=128"
                },
                "保險局": {
                    "jurisdiction": ["人壽保險", "產物保險"],
                    "enforcement_actions_url": "/ch/home.jsp?id=139"
                }
            }
        },
        "中央銀行": {
            "full_name": "中華民國中央銀行",
            "english_name": "Central Bank of the Republic of China",
            "jurisdiction": ["貨幣政策", "外匯管理", "金融檢查"],
            "website": "https://www.cbc.gov.tw/"
        },
        "公平會": {
            "full_name": "公平交易委員會",
            "english_name": "Fair Trade Commission",
            "jurisdiction": ["反壟斷", "不正當競爭", "多層次傳銷"],
            "website": "https://www.ftc.gov.tw/"
        },
        "財政部": {
            "full_name": "財政部",
            "english_name": "Ministry of Finance",
            "jurisdiction": ["稅務", "關稅", "國有財產"],
            "website": "https://www.mof.gov.tw/"
        }
    }

    def get_regulator_by_violation_type(self, violation_type: str) -> List[str]:
        """
        根據違規類型判定應查詢哪些監管機構。

        範例：
            "洗錢防制" → ["金管會", "中央銀行"]
            "內線交易" → ["金管會"]
            "不當銷售" → ["金管會", "公平會"]
        """
        mapping = {
            "洗錢防制": ["金管會", "中央銀行"],
            "內線交易": ["金管會"],
            "證券詐欺": ["金管會"],
            "不當銷售": ["金管會", "公平會"],
            "利益衝突": ["金管會"],
            "資訊揭露不實": ["金管會"],
            "市場操縱": ["金管會"],
            "外匯違規": ["中央銀行"],
            "聯合行為": ["公平會"],
        }

        return mapping.get(violation_type, ["金管會"])  # 預設金管會

    def get_enforcement_actions_url(self, regulator: str,
                                    sub_agency: Optional[str] = None) -> str:
        """取得裁罰案件的 URL。"""
        reg_info = self.REGULATORS.get(regulator, {})

        if sub_agency and "sub_agencies" in reg_info:
            sub_info = reg_info["sub_agencies"].get(sub_agency, {})
            return reg_info["website"] + sub_info.get("enforcement_actions_url", "")

        return reg_info.get("website", "")
```

### 10.3 日期格式處理

```python
class TaiwanDateConverter:
    """民國與西元年份轉換。"""

    @staticmethod
    def minguo_to_ad(minguo_year: int) -> int:
        """
        民國年轉西元年。

        範例：
            minguo_to_ad(110) → 2021
        """
        return minguo_year + 1911

    @staticmethod
    def ad_to_minguo(ad_year: int) -> int:
        """
        西元年轉民國年。

        範例：
            ad_to_minguo(2021) → 110
        """
        return ad_year - 1911

    @staticmethod
    def parse_taiwan_date(date_str: str) -> Optional[date]:
        """
        解析台灣日期格式。

        支援格式：
        - "民國110年5月15日"
        - "中華民國110年5月15日"
        - "110年5月15日"
        - "110/05/15"
        """
        patterns = [
            (r'(?:民國|中華民國)?(\d{2,3})年(\d{1,2})月(\d{1,2})日', True),
            (r'(\d{2,3})/(\d{1,2})/(\d{1,2})', True),
            (r'(\d{4})年(\d{1,2})月(\d{1,2})日', False),  # 西元
        ]

        for pattern, is_minguo in patterns:
            match = re.match(pattern, date_str)
            if match:
                year, month, day = map(int, match.groups())
                if is_minguo:
                    year = TaiwanDateConverter.minguo_to_ad(year)
                return date(year, month, day)

        return None

    @staticmethod
    def format_taiwan_date(d: date, use_minguo: bool = True) -> str:
        """
        格式化為台灣日期格式。

        範例：
            format_taiwan_date(date(2021, 5, 15), use_minguo=True)
            → "民國110年5月15日"
        """
        if use_minguo:
            minguo_year = TaiwanDateConverter.ad_to_minguo(d.year)
            return f"民國{minguo_year}年{d.month}月{d.day}日"
        else:
            return f"{d.year}年{d.month}月{d.day}日"
```

---

## 11. 成功標準

### 11.1 功能需求（必須具備）

- ✅ 準確檢索來自 5+ 監管機構的裁罰案件
- ✅ 處理最多 200 頁的 PDF
- ✅ 為所有來源生成正式台灣法律引用
- ✅ 提供 ≥90% 引用完整性的答案
- ✅ 識別 ≥70% 相關性的判例案件
- ✅ 在 <3 分鐘內完成典型查詢
- ✅ 優雅地處理超出範圍的查詢（空任務清單）
- ✅ 正確處理繁體中文文件與查詢

### 11.2 品質指標

```yaml
引用品質:
  - 引用完整性: ≥90%
  - 引用格式準確性: ≥95%（台灣法律格式）
  - 主要來源使用率: ≥60%
  - 有效 URL: ≥98%

答案品質:
  - 事實準確性: ≥95%（人工審核）
  - 查詢對齊: ≥85%（回答所問問題）
  - 適當信心: 80%（信心與實際準確性匹配）

繁體中文品質:
  - 無簡體字: 100%
  - 正確法律術語: ≥95%
  - 適當的正式語氣: ≥90%

性能:
  - 平均查詢時間: <3 分鐘
  - 文件處理: 每 100 頁 PDF <30 秒
  - 系統正常運行時間: ≥99%
  - API 錯誤率: <1%
```

### 11.3 使用者滿意度

```yaml
可用性:
  - 清晰的答案結構（摘要 → 發現 → 分析 → 引用）
  - 易讀的引用格式（台灣標準）
  - 匯出至 PDF/DOCX 穩定運作
  - 有用的錯誤訊息（繁體中文）

信任:
  - 來源可驗證（有效 URL）
  - 信心分數經過校準
  - 清楚說明限制
  - 優先使用主要來源
```

---

## 12. 未來增強功能

### 12.1 第二階段功能

```yaml
進階 RAG:
  - 跨文件的多跳推理
  - 自動引用驗證（檢查 URL、日期）
  - 跨文件一致性檢查

擴展涵蓋範圍:
  - 地方法院判決（各地方法院）
  - 行政訴訟案件
  - 仲裁判斷
  - 和解協議資料庫

AI 能力:
  - 預測性罰款建模（估計可能的罰款）
  - 趨勢分析（隨時間變化的執法模式）
  - 機構風險評分
  - 符合標準的新裁罰案件自動警報系統

使用者功能:
  - 儲存的研究會話
  - 協作研究（多使用者）
  - 自訂報告範本
  - 符合標準的新案件電子郵件警報
```

### 12.2 整合機會

```yaml
企業整合:
  - 匯出至法律研究管理系統
  - 與合規管理平台整合
  - 第三方應用程式的 API

資料合作夥伴:
  - 即時裁罰案件提要
  - 專有罰款資料庫
  - 產業聯盟資料共享

台灣特定整合:
  - 會計師事務所系統
  - 律師事務所案件管理系統
  - 銀行合規系統
```

---

## 13. 附錄

### 13.1 關鍵差異摘要：Dexter → 法律研究代理

| 組件 | 變更 | 理由 |
|------|------|------|
| **文件處理** | 添加 RAG 管線 | 法律文件 100+ 頁 vs. Dexter 的 JSON |
| **引用** | 正式台灣法律格式 | 法律精確性需要正確引用 |
| **工具** | 20+ 工具（vs. 15） | 多個管轄區域和文件類型 |
| **驗證** | 引用完整性 | 法律研究需要所有陳述都有來源 |
| **分塊** | 語義（章節感知） | 保留法律文件結構 |
| **查詢澄清** | 考慮添加 | 法律精確性可能證明詢問問題是合理的 |
| **時間邏輯** | 判例回溯 | 只有先前的案件是有效判例 |
| **信心評分** | 基於來源權威 | 法律領域主要 > 次要來源 |
| **匯出** | PDF 帶引用 | 法律專業人士需要格式化報告 |
| **上下文視窗** | 添加 RAG（不只是 LLM） | 文件對於上下文視窗太大 |
| **繁體中文** | 全面支援 | 針對台灣文件與查詢優化 |

### 13.2 術語表

```yaml
AML: 洗錢防制（Anti-Money Laundering）
BSA: 銀行保密法（Bank Secrecy Act）
CFPB: 消費者金融保護局（Consumer Financial Protection Bureau）
FSC: 金融監督管理委員會（Financial Supervisory Commission，台灣金管會）
CBC: 中華民國中央銀行（Central Bank of the Republic of China）
FTC: 公平交易委員會（Fair Trade Commission，台灣公平會）
RAG: 檢索增強生成（Retrieval-Augmented Generation）
NER: 命名實體識別（Named Entity Recognition）
OCR: 光學字符識別（Optical Character Recognition）

台灣特定術語:
民國: 中華民國紀年（1911年起算）
裁罰書: 監管機構的處罰文件
判決書: 法院判決文件
案號: 案件編號（例如：110年台上字第1234號）
主文: 判決的結論部分
事實: 案件事實陳述
理由: 判決的理由說明
```

### 13.3 參考資源

```yaml
官方資源:
  - 金管會: https://www.fsc.gov.tw/
  - 中央銀行: https://www.cbc.gov.tw/
  - 公平會: https://www.ftc.gov.tw/
  - 司法院法學資料檢索系統: https://law.judicial.gov.tw/
  - 全國法規資料庫: https://law.moj.gov.tw/

技術資源:
  - CKIP 中文處理工具: https://ckip.iis.sinica.edu.tw/
  - Jieba 中文分詞: https://github.com/fxsjy/jieba
  - LangChain: https://python.langchain.com/
  - OpenAI: https://platform.openai.com/

開發社群:
  - PyConTW（Python 台灣社群）
  - COSCUP（開源人年會）
  - g0v（零時政府）
```

---

## 文件元資料

```yaml
規格書版本: 1.0
最後更新: 2025年11月11日
作者: 系統架構師
狀態: 草稿待審核
目標實作時間: 2026年第一季
預估工作量: 12週（1位開發者）
語言: 繁體中文
適用地區: 台灣
```

---

**規格書結束**
